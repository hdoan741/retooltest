{
  "name": "test_retool",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild ./index.ts --bundle --outdir=dist --packages=external --platform=node --format=cjs --sourcemap",
    "test": "node --experimental-modules ./dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.19.10",
    "retoolrpc": "^0.1.1",
    "typescript": "^5.3.3"
  }
}
